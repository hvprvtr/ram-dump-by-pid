"""ram-pid-reader: чтение памяти процесса по PID через драйвер дампера физпамяти.

Логика разнесена на два слоя:
* ``core``    — чистые функции, не зависящие от memprocfs (тестируются на моках);
* ``backend`` — адаптер поверх memprocfs/LeechCore (работает только на Windows).
"""

__version__ = "0.1.0"
